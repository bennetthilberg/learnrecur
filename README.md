# LearnRecur

LearnRecur is a web-first spaced repetition practice app for small academic
skills. The product contract lives in `project_description.md`; the build order
lives in `roadmap.md`.

The current code is intentionally small: a disposable Next.js + Mantine design
language lab. It exists to tune typography, color, density, radius, and basic UI
states before serious product implementation begins.

## Stack In This Slice

- Next.js App Router + TypeScript
- Mantine UI
- Tabler icons
- Lexend via Google Fonts

## Local Setup

Install dependencies:

```bash
npm install
```

Start development:

```bash
npm run dev
```

Then open `http://localhost:3000`.

## Verification

```bash
npm run lint
npm run build
```

## Human Setup

See `human-stuff.md` for the external account/API-key setup checklist. Those
services are not wired into the current design lab.

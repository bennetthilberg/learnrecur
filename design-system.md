# LearnRecur Design System

`LearnRecur-OpenWaterIII-design-spec.md` is the authoritative design system for current frontend work.

This file intentionally does not restate tokens, typography, layout, or component rules. Keeping a second copy here caused stale Lexend/steel guidance to conflict with the approved Open Water III system.

For UI changes, read:

1. `LearnRecur-OpenWaterIII-design-spec.md`
2. `ui-todo.md` for the current cleanup audit

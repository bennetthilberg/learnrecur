# UI release improvements

Branch: `a/ui-polish`. Commit locally; do not push or open a PR.

All items below remain required. An implementation is not verified until its relevant automated and rendered checks pass.

## Requested scope

carefully, beautifully, delightfully implement all of those updates and do all of those checks, fixing anything the checks identify as needed. the list is pasted below:
Fix before the next release
1\. Protect checked answers when users leave Practice.
&#x20;  Checking shows feedback, but the review is only recorded after Continue. Leaving after seeing “Correct” silently abandons it. Preserve the pending review or offer a clear save/discard choice. Users should not need to know this implementation detail.
2\. Make slow-save navigation understandable.
&#x20;  While a review saves, app links are silently blocked. A user clicking Dashboard can reasonably think navigation is broken. Remember the requested destination and navigate after saving; show a clear recovery action if confirmation takes unusually long.
3\. Add consistent page-level error recovery.
&#x20;  I found a dedicated error boundary for Needs Attention, but not the other main routes. Add a branded error state with Retry and a useful escape route while retaining navigation. This is a source-backed gap; I did not deliberately crash the running app.
4\. Make “Start practice” on a skill page target that skill.
&#x20;  It currently opens the general practice queue. Link to Custom Session with that skill selected, or explicitly label the action “Open all practice.” The current placement promises more context than the destination delivers.
5\. Allow reporting a problem before answering—and in custom practice.
&#x20;  Normal practice exposes Report issue only after checking an answer; custom practice lacks the equivalent control. An unanswerable or broken question should not force a guess. Offer reporting before Check, with a clear explanation of whether the attempt affects the schedule.
6\. Correct the dashboard’s “Retention” metric.
&#x20;  It displays recent answer accuracy, not estimated memory retention. Rename it Recent accuracy, and provide the sample size/time window. Otherwise it appears directly comparable to the separate “Desired retention” setting when it isn’t.
7\. Protect unsaved form work.
&#x20;  Settings and creation inputs lack a general dirty-state/navigation safeguard. Preserve draft text and selections, or warn before discarding meaningful edits. Existing recovery for submitted sources does not protect notes that have not yet been submitted.
8\. Fix history’s essential content and truncated results.
&#x20;  I observed “Incorrect” shortened to “Incorr…” in the table. Review details showed “Correct answer: está” without the original sentence or the learner’s answer. Show question, your answer, correct answer, explanation together, and never truncate the correctness result.
Next improvements with the biggest everyday benefit
9\. Add search and filtering to Skills.
&#x20;  The current list works for two skills but has no search or collection/status filters. Add title search, collection filtering, and clear Active/Paused/Archived views. Test with a realistically large library rather than only sample content.
10\. Make skill organization editable.
&#x20;   The current detail-page Edit action changes practice guidance, not the skill’s title or collection. Provide obvious Rename and Move to collection actions. Users should be able to correct organization without recreating a skill.
11\. Make custom-session filters explain their result.
&#x20;   Collection, tags, recently missed, and explicitly selected skills can interact, but the screen does not preview the matching set. Show an eligible-skill count, update the list as filters change, and explain an empty match before Start session. Rename “Selected skills” to “Choose skills” when nothing is selected.
12\. Make mobile navigation discoverable and stable.
&#x20;   Some destinations disappear beyond the horizontal edge without an obvious overflow cue. The active destination also moves toward the front, changing the learned order. Keep a stable order and provide an unmistakable way to reach the remaining destinations.
13\. Make History useful beyond the latest reviews.
&#x20;   The page loads the latest 50 reviews with no pagination or filtering. Add Load more and filters for skill, collection, and incorrect answers. Otherwise users cannot reliably find an older mistake.
14\. Give completion states a useful next step.
&#x20;   “You’re all caught up” should ideally show the next scheduled review time and offer optional custom practice. Clearly distinguish:
&#x20;   \- Nothing due.
&#x20;   \- Daily new-skill allowance reached.
&#x20;   \- Exercises still being prepared.
&#x20;   These situations require different actions and should not feel like interchangeable dead ends.
15\. Simplify the first material-import experience.
&#x20;   With no saved materials, an empty “Reuse a material” panel takes almost half the page while the actual import form becomes cramped. Give importing the main space until reusable materials exist; stack title and collection fields before they become narrow.
16\. Use one vocabulary for the product’s objects.
&#x20;   Collections are also called “study areas,” and the Collections page is titled “Organize practice.” Standardize the visible names. Briefly explain the relationship once: materials provide source content, skills are practiced, collections organize skills.
Polish after those workflow fixes
17\. Bring Dashboard into line with the revised practice UI.
&#x20;   Its preview still renders instructions and exercise text together in bold, with raw-looking underscores. Reuse the practice prompt renderer. Remove promotional/mechanical copy such as “Instant check” and “update the memory schedule.” Also replace “+ New skill” under Collections with an action that matches that section.
18\. Reduce empty detail-page sections.
&#x20;   A skill with no saved guidance gets separate Rules, Examples, and Exercise focus blocks explaining that each is empty, followed by result sections with no reviews. Collapse these into compact empty states and prioritize schedule, useful results, and actions.
19\. Make defaults and timezones easier to understand.
&#x20;   Practice uses UTC in the inspected account while reminders use America/New\_York. Separate timezones can be valid, but make the difference explicit and offer “Use practice timezone” for reminders. Replace disabled placeholder-looking values with clearer summaries such as “Unlimited” or “Using default: 90%.”
20\. Replace instruction detection with structured prompt content over time.
&#x20;   The lighter instruction treatment currently recognizes certain English prefixes and punctuation. Other wording or languages can retain the old hierarchy. Store instruction and exercise content separately for new exercises, keeping a safe fallback for existing ones.
Release checks still needed
These are verification tasks, not confirmed defects:
\- Accessibility: screen-reader labels for modal close buttons, keyboard focus after navigation/feedback/errors, and announcements when the next question appears.
\- Interruption testing: offline saves, expired sessions, browser Back during saving, refresh during creation, and reopening an unfinished custom session.
\- Long-running imports: large files, partial generation failures, retry without duplication, and clear guidance on whether it is safe to leave.
\- Real-device layouts: mobile keyboards, long skill names, long answer choices, math expressions, and increased text size.

## Evidence ledger

- Work started September 14, 2026. No items marked complete yet.

### First implementation checkpoint

- Item 6: dashboard now says Recent accuracy and displays answer count plus the shared 14-day query window. Existing query semantics unchanged.
- Item 17: dashboard uses PracticePrompt (including lighter instructions and formatted blanks), removes mechanical promotional copy, and links Manage collections to Collections.
- Item 4: skill detail now links Practice this skill to `/practice/custom?skillId=...`. Existing setup resolves only active owned skills. End-to-end preselection verification still required.
- Item 3: shared PageError wired to Dashboard, Practice (including Needs Attention), History, Skills, Collections, and Settings. Retains topbar, focuses heading, offers Retry and a different route. Unit interaction test passed; deliberately induced route failures and rendered error screenshots still required.
- Checks: TypeScript passed; targeted ESLint passed; all 1,100 unit tests passed; dashboard clarity authenticated Playwright passed (plus setup/cleanup), checking overflow at 390/1000/1280. Dashboard inspected in browser at compact and narrow widths. Full build, database and release-wide browser checks remain outstanding.
- Design review: removed inaccurate metric naming, overly bold preview instructions, raw underscores, mechanical explanatory copy, and the mismatched Collections CTA. Retained existing type, colors, card and button styles.

Next: implement checked-answer preservation and save-aware navigation together, including custom practice. Preserve idempotent attempt identities, server-authoritative next-item selection and the single-outstanding-save invariant. Current `use-review-save-guard.ts` only silently blocks links while saving; both clients reset to a preview before save acknowledgement. A recovery solution must retain the original checked review as well as any new answer typed into the preview, and must cover Back/refresh/expired auth/offline behavior.

Items 1, 2, 5, 7–16, 18–20 and the full release verification matrix remain open. Do not interpret this checkpoint as completion of the goal.

### History checkpoint

- Items 8 and 13 implemented: owned review details include question, submitted answer (choice IDs resolved to labels), correct answer, and saved explanation. Supports both legacy scalar answers and current `{ raw: ... }` storage without exposing arbitrary JSON. Correctness badges do not truncate; modal has a named close button and returns focus on close. Modal primary action is right-aligned.
- History now has searchable skill/collection pickers, incorrect-only filtering, clear empty results, and Load more. Queries remain user-scoped on every page and use reviewedAt/id keyset pagination (50 rows), preserving tied timestamps. Failed loads retain current rows and permit retry; duplicate clicks/rows are guarded.
- Updated loading layout to reserve filter space. Inspected real correct/incorrect history and narrow-screen filters; corrected a missing mobile inset found in that inspection.
- Verification: history integration suite passed (5 tests, including 52 tied-timestamp reviews, cross-user cursor use, nonexistent scope filters). History answer/browser unit tests plus input tests passed (10). Authenticated browser tests passed for review detail/export isolation, skill-specific preselection, and 52-review pagination with combined filters. The pagination test checks 390/1000/1280 overflow and badge text width.
- A pre-existing lifecycle assertion assumed whitespace between instruction/content DOM nodes; updated it to compare paragraph text joined with whitespace. It failed on the first run, then passed with the actual rendering contract.
- Release-wide baseline and checks remain pending. The next core task remains items 1/2: checked-answer preservation and save-aware navigation. No changes to those clients yet. Other open items: 5, 7, 9–12, 14–16, 18–20 and full release verification.

### Practice recovery checkpoint

- Items 1/2 implemented for normal and custom practice: tab-local recovery is keyed by authenticated account and practice scope/session. Loaders restore unfinished answers before asking for a new question. Snapshots retain the original attempt identity, checked feedback inputs, rating and response time, with the in-flight review separate from the current preloaded answer. Malformed/oversized stored data is rejected; server checking and eligibility remain authoritative.
- If a save fails after the next answer has been entered, that next answer is retained separately. A retry restores it only if the authoritative next item matches. Lost acknowledgements remain idempotent. Storage failure enables a leave/discard warning instead of silently discarding the answer. Recovery is tab-local (not cross-device or permanent storage).
- App navigation during a save now remembers the clicked destination, explains the wait, and opens it after successful acknowledgement. Failed confirmation keeps the destination for a retry. A 15-second confirmation deadline restores the checked answer and enables retry; late responses cannot overwrite the recovered UI. Server operations still use the original identity on retry.
- Added a compact restored-answer notice and an explicit reload action to leave an obsolete answer. Inspected the notice at compact and narrow widths, and fixed the restored timer display to use the saved response time. Cleared the inspection-only answer without submitting a review.
- Verification: all 1,112 unit tests passed; targeted lint and TypeScript passed. The fluid-practice suite passed 11 scenarios plus setup/cleanup, including normal/custom fast advances, authoritative stale-buffer replacement, lost-ack retry, navigation/refresh recovery, queued destinations, and preservation of the next checked answer across failure plus reload. Additional interruption tests passed normal/custom Back during an in-flight save and a real 15-second confirmation timeout (3 scenarios plus setup/cleanup). Initial text assertions failed because innerText and textContent differ across the prompt's two paragraphs; fixed the assertions to compare the same DOM representation and reran successfully.
- Full release audit remains open, including actual offline/expired-auth scenarios and all outstanding feature items. Next implementation: reporting before Check and in custom practice (5), then unsaved form recovery (7), Skills search/editing (9/10), filter previews/navigation/completion/materials/copy/defaults/structured prompts (11/12/14/15/16/18/19/20). Main-route error rendering checks (3) and release-wide build/database/browser baseline also remain required.

### Reporting checkpoint

- Item 5: Report issue is available before Check and after feedback in normal and custom sessions. Both explain that reporting removes the exercise without recording an answer or changing the review schedule. Custom reports stay in the same session and use its current presented item; they do not call the normal queue or create a review. Pending saves disable reporting.
- Custom server action validates authenticated session ownership, active session status, and the presented item/exercise pair before calling the existing report/refill path. The next custom item is resolved from the existing session plan after retirement. Added rejection tests for foreign/missing sessions and mismatched exercises.
- Report submission errors retain reasons/notes and allow retry. Custom success uses the existing notification component. A successful report clears obsolete recovery drafts. Corrected a wrapping Close report button and restored focus to Report issue when closing the normal form.
- Verification: all 1,114 unit tests passed; targeted lint and TypeScript passed. Browser tests prove no attempt, no review log and unchanged due date/repetitions/lapses/state in normal, custom practice-only and custom scheduled review. First browser run failed on a test query that assumed exercise_flags contained skillId; corrected the query through exercises and reran. Inspected the real normal form without reporting the user's exercise, plus generated desktop/mobile screenshots of custom reporting. Focus-return browser assertions are included in the final focused rerun.
- Remaining implementation: 7, 9–12, 14–16, 18–20. Remaining verification includes main-route induced errors (3), offline/expired auth, form and import interruption cases, large/partial imports with idempotent retries, expanded accessibility/long-content/keyboard/zoom checks, and the full release baseline. Next: preserve unsaved settings and creation fields using account-scoped drafts or clear leave/discard protection; preserve submitted-source recovery and exclude credentials/files from generic text persistence.

### Material entry and terminology checkpoint

- Re-read the full goal objective after the read-only audit. The audit produced current rendered evidence of first-import crowding, mobile navigation ordering, confusing defaults, and remaining skill-management gaps; implementation resumes with the original 20-item scope unchanged.
- Items 15/16 implemented: the first import omits the empty reuse panel and gives Add a material the full working area. Existing materials still expose their reuse list. Title/collection fields use an intrinsic minimum width and stack before becoming cramped. The initial import skeleton matches the first-use form rather than inventing existing materials.
- Collections now uses Collections/Add a collection throughout its page, form placeholder and loading state. Add explains materials, skills and collections once in its introduction. Also corrected the Dashboard loading label from Retention to Recent accuracy.
- Verification: 24 focused loading/style unit tests passed; focused ESLint passed; TypeScript passed, including the final recheck. Authenticated Playwright passed both new scenarios plus setup/cleanup (4 total): first-use layout at 390/820/1000/1280, field widths, no page overflow, Website tab access, and consistent Collections/Add terminology. Inspected generated 390/1280 screenshots; mobile fields stack, desktop form fills its parent, primary action remains right-aligned on desktop.
- Design critique and fixes: removed the empty competing reuse card; widened the actual task area; replaced cramped equal fields with content-sensitive wrapping; removed competing names for Collections; replaced stale loading copy. Preserved existing fonts, colors and surface/button treatment.
- Remaining implementation: 7, 9–12, 14, 18–20. All previously listed release-wide error, interruption, import, accessibility and long-content checks remain required. No push or PR.

### Skill detail density checkpoint

- Item 18 implemented: missing guidance becomes one compact message with Edit retained. Partially populated guidance shows only saved sections. Practice results omit answer types without completed reviews. Empty recent-review and source panels are omitted, avoiding duplicated empty messaging.
- Moved results and recent reviews directly below Schedule in the document order. Removed named grid rows that otherwise reserve gaps for omitted content; visual and keyboard reading order now agree. Empty cards omit unnecessary descriptive copy/dividers.
- Verification: the new authenticated test failed on the original three-empty-section UI, then passed after implementation. A subsequent populated-guidance assertion exposed an incorrectly shaped test fixture (array instead of stored `{ items: [...] }`); corrected the fixture and reran. Final browser scenario checks empty/partial guidance, one empty review summary, an actual saved choice review with no unused result rows, and no overflow at 390/1280. Setup/scenario/cleanup all passed. Inspected final narrow screenshot and earlier desktop screenshot; long fixture title/collection wrap. All 1,114 unit tests passed; final TypeScript and focused ESLint passed.
- Design critique/fixes: removed three repeated missing-guidance messages, removed zero-data answer-type rows, removed duplicated no-review panels, removed decorative dividers in empty cards, and removed layout gaps/reading-order mismatches caused by fixed grid areas.
- Remaining implementation: 7, 9–12, 14, 19/20. All full-release checks listed above remain open; this is a verified checkpoint, not release completion. No push or PR.

### Settings defaults and timezones checkpoint

- Item 19 implemented: unlimited new skills displays a read-only Unlimited summary instead of a disabled 20. Default retention displays Using default: 90%; unchecking the option reveals the editable numeric value. Existing limits, fractional retention and save semantics are preserved.
- Reminder settings receive the saved practice timezone, explain its daily-allowance role separately from email delivery, and offer Use practice timezone. The shortcut changes only the form until Save changes; it includes timezones outside the short reminder preset list. Successful saves display the server-returned timezone. No reminder opt-in occurs from the shortcut.
- Verification: new timezone test failed before implementation, then passed: copies Asia/Kolkata, discards an unsaved copy on reload, persists after Save changes, and keeps reminders off. Existing retention reset/fractional-value cases passed. Daily-limit cases initially failed on an older assertion expecting one prompt text node; corrected it to compare both rendered prompt paragraphs. Both desktop/mobile daily-limit cases then passed, including practice gating. Across the runs all 5 relevant scenarios passed with setup/cleanup. All 1,114 unit tests, final TypeScript and focused ESLint passed. Inspected desktop settings and mobile reminder screenshots, with no overflow at 390/1280.
- Design critique/fixes: removed a misleading disabled 20, replaced a faint default percentage with an explicit summary, explained the two timezone purposes, avoided forcing users to search for the same timezone twice, and kept the shortcut secondary to Save changes. Existing visual language retained.
- Remaining implementation: 7, 9–12, 14, 20. Full release verification remains required. Next substantial workflow: protect unsaved settings and creation inputs, including refresh/navigation and file-input limitations. No push or PR.

### Settings draft recovery checkpoint

- Item 7 partially implemented for practice preferences (account/collection/skill scopes) and reminder settings. Explicit typed draft fields persist to account/scope-keyed sessionStorage synchronously when edited. The shared helper validates envelopes, rejects oversized/malformed values, and discards stale drafts when saved server values change. No account email, credentials or file bytes are stored. Closing the tab is outside this tab-local guarantee.
- Inputs remain disabled until restoration completes. A persistent notice distinguishes unfinished changes from saved preferences and offers Discard changes. It sits near Save so the first keystroke does not move the input. Confirmed saves clear the stored draft; failed saves retain it. Reminder drafts include time/hour/threshold but do not opt into email without the existing toggle/save action. Removed a separate reminder copy of saved props so refreshed server values remain authoritative.
- Storage failures show explicit save-before-leaving guidance and protect normal app-link/full-document navigation with a leave warning. Client-side browser Back while storage is unavailable remains an interruption edge case for the release audit; do not treat that fallback as fully verified.
- Verification: storage/hook tests cover account/scope isolation, stale baseline, malformed/oversized payloads, acknowledgement/discard, and denied-storage leave warnings. All 1,119 unit tests passed. Initial full unit run exposed missing Clerk context in two existing server-render tests; added an authenticated hook mock and reran. Final TypeScript and focused lint passed. Combined authenticated run passed all 7 scenarios plus setup/cleanup (9 total): navigation/Back/refresh recovery, failed save, discard, successful persistence, reminder draft retention, daily-limit gating at 390/1280, retention reset and fractional retention preservation. After the last reminder props correction, both reminder scenarios were rerun and passed (4 including setup/cleanup). Inspected narrow draft notices and settings screenshots; final reminder notice is below inputs.
- Design critique/fixes: avoided automatic saving disguised as draft recovery, avoided a generic serializer capturing account fields, removed a first-keystroke layout jump, retained readable pending-save copy, and kept a clear secondary discard action near Save.
- Item 7 remains open for creation inputs, file selections and draft-review forms. Next: source-creation-workspace MaterialSnapshot currently uses uncontrolled fields and only captures the form during transitions/submission; integrate typed text drafts without breaking submitted-source restoration or pretending browser file bytes survive refresh. Other remaining implementation: 9–12, 14, 20. Full-release verification still required. No push or PR.

### Source entry draft recovery checkpoint

- Item 7 now covers unfinished single-skill source text, source name, collection, focus and tags. Typed account-scoped tab drafts restore after navigation/Back/refresh; discard clears them and successful activation clears the source draft. No file bytes are persisted.
- File names survive refresh as explicit reselection reminders. Create stays disabled while any file is missing; users can deliberately continue without missing files. Partial reselection retains the remaining names, including duplicate filenames. Actual browser File objects remain the only upload source.
- Verification: new partial-reselection browser assertion failed before the fix, then passed. Both authenticated scenarios plus setup/cleanup passed (4 total), covering text/context navigation, refresh, discard, missing-file gate and partial reselection. A test initially matched both a hidden loading label and the real More options label; scoped it to the actual form and reran. All 1,121 unit tests passed; TypeScript and focused lint passed. Inspected final desktop 1280 and mobile 390 screenshots with no overflow.
- Design critique/fixes: removed ambiguous Save wording from creation recovery, avoided pretending files survived refresh, prevented partial reselection from silently dropping files, retained a persistent secondary discard action, and kept the primary Create action at the right without changing the brand.
- Item 7 remains open for material import inputs, website page selections, collection forms and draft-review editors, plus refresh during generation. Remaining feature implementation: 9–12, 14, 20. Full release verification remains open. The read-only audit also identified nested loading mismatch, skill-history context loss, inconsistent reminder saving and custom-practice focus handling for the remaining checks. No push or PR.

### Material import draft recovery checkpoint

- Item 7 now covers reusable PDF title/collection and website URL/title/collection/discovered page selections. PDF and Website panels stay mounted when switching tabs, preserving browser file selections as well as typed values. Refresh restores typed metadata and names any PDF that must be reselected; Import PDF is disabled without an actual file.
- Website drafts contain validated metadata and HTTPS links, not fetched page bodies. Server import validation remains authoritative. Successful import acknowledgement clears the stored draft; a failed website request shows a retryable error and retains the selection. Discard remains explicit. Recovery copy distinguishes an undiscovered URL from a page selection.
- Verification: the new browser test first failed on the original PDF title reset after changing tabs. Final authenticated run passed 4 scenarios plus setup/cleanup (6 total): tab/file retention, navigation/refresh/discard, seeded website page selection restoration and edits, aborted import recovery, and existing first-import layout checks at 390/820/1000/1280. The seeded website test verifies frontend recovery, not live discovery or successful server import. All 1,125 unit tests passed, including unsafe restored-link rejection and exclusion of page bodies; TypeScript and focused lint passed. Inspected 390/1280 restored-import screenshots, then corrected the notice for URLs without discovered pages.
- Design critique/fixes: stopped clearing files on source-tab changes, separated restored text from missing file bytes, avoided implying undiscovered pages were already selected, retained explicit discard near the task, and replaced silent network rejection with a recoverable error. Preserved the established layouts and visual language.
- Item 7 remains open for collection forms, source/draft review editors, and refresh during generation. Remaining feature implementation: 9–12, 14, 20; full release verification remains required. No push or PR.

### Collection and skill-editor draft checkpoint

- Item 7 now covers collection creation/editing and the shared skill-draft editor used on draft detail pages, single-source review and batch edit dialogs. Account/scope-keyed typed drafts preserve editable fields across unmount/navigation/refresh; Discard restores the last acknowledged values. Collection creation clears after acknowledgement, edit saves clear their recovery notice, and collection request failures retain the form with a retryable error.
- Skill drafts use controlled fields, including familiarity for manual creation. Server submission and activation behavior remain unchanged. A confirmed save-only batch edit persists the skill without activating it; unfinished edits do not mutate the saved database row. Recovery copy names the real next action (Save or Add), and hydration disables fields without pretending a save is running.
- Verification: collection and skill-detail recovery tests each failed on refresh before implementation. Final combined authenticated suite passed all 3 scenarios plus setup/cleanup (5 total): collection refresh/create/failure/discard/update, draft navigation/refresh/discard plus database non-mutation, and batch modal close/reopen/save plus database DRAFT status and no stale notice after reload. One initial batch run timed out at 30 seconds without a useful page snapshot; a traced rerun with a 90-second allowance passed in 10.1 seconds and the final combined run passed. No claim about the cause of that initial timeout.
- The first narrow skill-editor check found long fixture title overflow; added natural word breaking to draft headings and reran. Inspected collection 390 and skill-editor 390/1280 screenshots. The collection screenshot caught a toast mid-transition after resize; settled-toast bounds remain part of the accessibility/responsive release check. Full-page desktop captures also retain the viewport-fixed sidebar at the capture scroll position, so do not interpret that as a sidebar layout shift.
- All 1,125 unit tests passed; TypeScript and focused lint passed. An intermediate hydration-disable edit accidentally touched the separate duplicate-decision component; TypeScript and its unit test caught the out-of-scope variable reference, which was corrected before the final passing runs. No backend/domain implementation changed.
- Design critique/fixes: prevented silent loss of form edits, kept explicit Discard secondary, avoided misleading Save wording on Add flows, kept failed collection saves editable, and fixed long-title overflow without shrinking text or changing branding.
- Item 7 still needs active-skill guidance edits, batch request/scope input recovery, and generation-interruption verification. Other remaining feature implementation: 9–12, 14, 20. Full release baseline and previously listed interruption, accessibility, import and responsive checks remain required. No push or PR.

### Guidance and batch-request recovery checkpoint

- Item 7 now covers active-skill guidance and both initial/revised batch request text. Guidance restores after modal close and refresh, preserves edits on failed saves, clears the recovery notice after acknowledgement, and names its close button. Closing is disabled during save; focus returns to Edit after closing.
- Batch request drafts are keyed by material revision or existing batch. Initial requests retain their client request identity across refresh/Back; changing the request text creates a new identity. The existing server idempotency contract is unchanged. Restored text has a clear discard action; labels remain explicitly associated and controls are disabled while restoring or submitting.
- Verification: authenticated guidance and batch-request scenarios plus setup/cleanup passed (4 total), including guidance save/reload/focus return, request text/identity restoration, identity change on edit, discard, and database confirmation that the no-submit test created no batch. This verifies frontend identity preservation, not live generation retry semantics. All 1,125 unit tests passed; TypeScript and focused lint passed. Existing textarea unit tests mock the shared draft helper and still cover pending behavior.
- The first guidance test had an overly exact accessible-name match (the existing label also includes its help text); corrected the locator. A later parallel run exceeded its five-second save assertion while the dialog still correctly showed Saving. The final run passed with a fifteen-second save assertion, then passed again with mobile Save visibility checks. No conclusion about backend latency beyond that observation.
- Inspected guidance and batch-request layouts at 390/1280. Verified the guidance Save button is reachable within the scrollable modal. Added spacing/wrapping between the batch limit hint and primary action after rendered inspection found them touching.
- Design critique/fixes: prevented modal edits from disappearing on close, avoided a fresh retry identity after refresh, kept Discard secondary, supplied a named modal close control with focus return, and separated the batch action from its hint without changing the visual language.
- Item 7's remaining evidence includes storage-unavailable Back behavior, refresh during generation, and completed-generation recovery. Custom-session setup preservation should be covered with item 11. Remaining major feature work: Skills search/filtering and organization (9/10), custom filter preview (11), mobile navigation (12), completion next steps (14), and structured prompts (20). All release-wide verification remains required. Next concrete action: implement Skills search, collection/status filters and a large-library browser fixture. No push or PR.

### Skills library filtering checkpoint

- Item 9: Skills now has immediate title search, collection filtering by stable collection ID (including Uncollected), and Active/Paused/Archived/All status views. Filters combine, matching counts and empty states explain results, and 50-row Load more keeps long lists manageable. URL state survives opening a skill, browser Back, and refresh without fetching on each keystroke.
- Automated evidence: all 1,127 unit tests passed; authenticated browser test seeded 180 owned skills across three statuses and verified pagination, combined filters, empty/reset behavior, and Back/refresh restoration. Final browser run passed all 3 tests including isolated-user setup/cleanup. TypeScript, targeted ESLint, and diff whitespace checks passed.
- Rendered evidence: inspected 390px and 1280px screenshots, with overflow checks also passing at 820px. Long titles wrap and row actions remain accessible. The fixture created no exercises/jobs and cleaned up its records. Existing mobile active-item reordering remains for item 12.
- Design critique and fixes: rejected browser-default Clear styling (used Mantine UnstyledButton); mismatched input heights (matched search size to Select); a wall of 180 rows (Load more); one combined recovery section (separate status views); and an unexplained empty list (matching count, explicit empty guidance, and clear-search action). Preserved type, palette, flat panels, and existing row actions.
- No push or PR. Item 10 skill organization is next. Items 11, 12, 14, 20 and outstanding interruption/accessibility/import/full-release checks remain required; this checkpoint is not completion of the goal.

### Skill organization checkpoint

- Item 10: active and paused skill detail pages expose Rename skill and Move to collection. Drafts retain their existing full editor; archived skills retain the existing restore-before-edit contract. Guidance's former generic Edit action now says Edit guidance. Organization changes use the existing updateSkillMetadata transaction, ownership checks, and collection-policy invalidation rather than duplicating domain logic.
- Rename accepts a trimmed nonempty name up to 120 characters; the action submits only the field being edited. Move offers owned active collections plus Uncollected. A current archived collection remains visible as a disabled option so its identity is not silently lost. Review history and schedule are preserved; moving across text policies retires incompatible future text inventory through the existing helper.
- Both dialogs retain unfinished drafts, recover failed network saves without closing, label their close controls, and return keyboard focus to the trigger. A success notice is dismissed before opening another organization dialog so it cannot obscure the mobile modal header.
- Verification: 1,128 unit tests passed; 2 new database integration cases passed (rename/move/uncollect with schedule readback, foreign-owner and archived/missing-destination rejection). Existing cross-text-policy move integration test passed, including preservation of historical evidence. Combined organization/guidance/detail browser run passed all 5 tests; final organization rerun after notice fix passed 3 including setup/cleanup. TypeScript, targeted ESLint, and whitespace checks passed. 390px/1280px move dialogs visually inspected; no overflow in either width. Initial detail test failure was an old exact copy assertion after Edit became Edit guidance; updated the assertion and reran successfully.
- Design critique and fixes: generic Edit label now names guidance; organization controls are visible by the skill summary; modal actions keep the primary action on the right; success notices no longer repeat their heading in the message; and previous notices no longer obscure the next dialog on mobile. Existing brand/type/styles retained.
- No push or PR. Next: item 11 custom-session filter previews and remaining setup draft protection. Items 12, 14, 20 and the release verification matrix remain required. Goal remains active.

### Custom-session filter preview checkpoint

- Item 11: Choose skills now previews the intersection of collection, all selected tags, recent misses (the same 30-day inclusive window used by session planning), and due status for scheduled mode. Setup only loads owned active skills with initialized scheduling fields, matching the planner's skill prerequisites. Count includes explicit selections; filtered-out selections remain saved with an exclusion message and a Clear skill selection action. Empty matches explain how to recover and disable Start. Exercise availability remains server-checked at session creation and is explicitly distinguished from skill eligibility.
- Remaining item 7 setup gap: custom choices now use account-scoped typed tab drafts, with a separate scope for an explicit skill prefill. Fields survive refresh and failed-start requests; successful session creation clears the draft. Inputs stay disabled during restoration/submission. Exercise count permits temporary empty edits and validates a whole number from 1–100 on submission.
- Checks: all 1,130 unit tests passed; combined authenticated custom-setup and existing custom-practice flow passed 4 tests including setup/cleanup. Browser evidence covers due-only exclusion, tags/collection/recent-miss filtering, preserved hidden selection, refresh restoration, count editing, failed-start retention, and discard. Existing custom-practice test starts a real session and verifies practice-only does not update FSRS. TypeScript, targeted ESLint, and whitespace checks passed.
- Rendered at 390/1280px with long fixture titles; overflow checks passed. Tightened paragraph margins after initial inspection and separated draft notice from the last skill. Existing custom-practice checks also cover 375px. Counts describe the loaded setup snapshot; the server remains authoritative at Start.
- Design critique/fixes: misleading Selected skills heading replaced with Choose skills; unchanged list now responds to filters; empty results now explain recovery; hidden selections are explicit rather than silently dropped; and a long paragraph/gaps were reduced so the matching list stays near its count. No new visual language.
- No push or PR. Next: item 12 stable/discoverable mobile navigation. Items 14, 20 and all remaining release checks remain required. Goal stays active.

### Compact navigation checkpoint

- Item 12: removed active-link reordering. Below the 1120px sidebar breakpoint, a labeled Pages menu exposes all seven destinations in the same stable order as the main navigation, highlights the current page, and supports keyboard opening/dismissal with focus return. The menu hides when the sidebar is visible. Mobile header uses explicit columns for brand, Pages, and account controls; the existing horizontally scrollable links remain available.
- Menu links reuse route prefetch and optimistic loading handling. The loading handler now locates the shell from the navigation ref, since a Mantine portal link is outside the shell DOM. Existing save/draft navigation interception continues to see ordinary links.
- Verification: all 1,130 unit tests passed, including route-prefetch behavior; TypeScript, targeted ESLint, and whitespace checks passed. Combined browser run passed 10 tests including isolated-user setup/cleanup, stable-order/menu/keyboard checks at 390/820/1119, and loading-to-content width comparisons at 390/720/1000/1119/1280. Final settled-menu screenshot run passed 5 tests. Visually inspected 390px and 820px menus; all destinations fit in view, active state is clear, and account/brand alignment is preserved.
- Initial unit failure was the test DOM's missing matchMedia after introducing Mantine Menu; provided the normal browser mock and Mantine provider. Initial screenshots captured transition opacity; final screenshots wait for loaded content and settled menu opacity rather than relying on arbitrary sleeps.
- Design critique/fixes: active links no longer change order; hidden destinations have an explicit labeled entry point; no icon-only navigation guessing; current destination remains identified in the menu; and portal navigation preserves the established loading layout. No new palette/type/design language.
- No push or PR. Next: item 14 completion states, then item 20 structured prompts and the remaining release verification work. Goal remains active.

### Practice completion checkpoint

- Item 14: completion shows the next active future schedule in the current collection/all-practice scope, formatted with the account's practice timezone and an explicit timezone label. When the daily allowance is exhausted, the future lookup follows the same previously-introduced-skill restriction as normal selection. The lookup runs only after no exercise can be selected.
- Caught-up and daily-limit states offer Custom practice. Daily limits retain Change daily limit with the server's limit/reset explanation. Preparation uses a wrench rather than a success checkmark and offers Check preparation, linking directly to the skill when only one requires preparation. Collection completion retains Try all practice. Removed the mechanical Queue/Schedule summary and duplicated preparation copy.
- Verification: 1,130 unit tests passed; new database integration test proves earliest future selection and collection scope. Browser test induces nothing-due, preparation-required, and zero-limit states and verifies dates/destinations; final run passed 3 tests including setup/cleanup. 390/1280px screenshots captured; completion/preparation/daily-limit mobile screenshots visually inspected, and overflow checks passed. TypeScript, targeted ESLint, and whitespace checks passed.
- Initial unit failure came from an existing disappearing-relation mock missing the new findFirst read; its empty result is now explicitly mocked and the complete unit suite passes. No grading/scheduling writes were changed.
- Design critique/fixes: replaced success icon on preparation problems; removed Queue/Clear for now metadata; replaced generic completion navigation with a useful practice option; made one-skill preparation navigation direct; and named the timezone instead of leaving a time ambiguous. Primary actions remain on the right and the visual system is unchanged.
- No push or PR. Next: item 20 structured instruction/content for new exercises, followed by remaining accessibility, interruption, import, device and full-release checks. Goal remains active.

### Structured prompt checkpoint

- Item 20 implemented for built-in generation: Gemini and fallback provider schemas now request separate instruction/content for choice, text/numeric, and math exercises. Validation composes one canonical prompt and requires stored parts to reproduce it exactly. Verification, answer checking, and history continue using that complete question. Existing legacy candidate inputs remain supported.
- Parts are stored under the existing exercise generationMetadata.promptLayout, preserving the existing provenance fields. No database migration is needed. Normal practice, rolling preload, custom sessions, Dashboard, and tab-local answer recovery carry only validated display parts, not private generation metadata. Missing or inconsistent parts fall back to the legacy renderer without altering the question.
- Verification: all 1,136 unit tests passed; two new database integration scenarios passed (real activation/publication, normal/custom/preload readback, corrupt-layout fallback, and text/math refill persistence). TypeScript and focused lint passed. Authenticated browser scenario plus isolated-account setup/cleanup passed all 3 tests, covering Dashboard, normal practice, selected-answer refresh recovery, custom session startup and refresh. Inspected 390px/1280px screenshots; no overflow and the French instruction uses the existing lighter hierarchy.
- The first candidate round-trip exposed a strict quality schema rejecting the new optional display field; added exact-text validation at that boundary. The first integration fixture omitted the required excludedSkillIds argument; corrected it. The first browser check timed out after five seconds while custom session startup still showed its loader. The final scenario uses a one-question session for its actual scope and a fifteen-second startup assertion; it passed. This is not evidence that large custom-session startup latency is resolved.
- Design critique/fixes: removed dependence on English instruction prefixes for new prompts; kept the instruction lighter without introducing another style; retained the established instruction/question gap; prevented stored metadata from replacing verified question text; and preserved the same hierarchy after refresh and across Dashboard/custom practice. No palette, font, or layout redesign.
- No push or PR. The goal remains active: remaining work includes storage-unavailable Back protection, generation/import interruption and retry checks, induced main-route errors, offline/expired-session recovery, custom keyboard focus/announcements, long-content/device/zoom checks, and the complete lint/unit/prisma/build/database/browser release baseline. The latest read-only audit also found reminder-save inconsistency, stale nested loaders, and context-losing History/collection links to address during release verification.

### Practice keyboard and modal accessibility checkpoint

- Normal and custom practice now move focus to the first choice or typed-answer field when advancing; custom Check moves focus to Save practice/Continue. A background acknowledgement does not steal focus from an unanswered next question. The shared prompt region announces changed question content politely and atomically. Custom typed answers support Enter; composition, held-key repeats, and modified keyboard commands do not trigger the shortcut. Normal shortcuts retain number selection when a choice button has focus.
- Added descriptive close names to connection revocation, skill archive, material deletion, and account deletion dialogs. No confirmation action or deletion behavior changed. Browser checks explicitly opened and closed account/archive dialogs without submitting them, verifying focus return. Existing custom report focus return also passed.
- Verification: new normal/custom focus tests failed before implementation. Final focused browser suite passed 6 tests including setup/cleanup: normal/custom advances, typed Enter and composition protection, report focus, and confirmation close names/focus. All 1,136 unit tests, TypeScript, full lint, Prisma validation, and diff whitespace checks passed. Inspected focused-next-question screenshots at 390/1280px. This verifies DOM focus and live-region contracts, not actual VoiceOver speech output.
- Broader fluid-practice run passed the rapid advance, stale-buffer, navigation, lost-response, and confirmation-timeout cases but had three timeouts in recovery cases. Sequential rerun passed the lost-response cases and custom Back case; normal Back intermittently failed waiting for page.goForward to load. An isolated normal Back rerun with tracing passed all 3 tests including setup/cleanup. Do not call the full fluid suite consistently green; retain that navigation timing issue for the production-build/full-release run. No assertion timeouts were widened in the existing suite.
- Design/accessibility critique and fixes: removed vanishing focus after custom feedback; supplied a next-choice focus target; made changed questions announceable; enabled Enter for typed custom answers without interfering with composition; and named the remaining modal close controls. Existing focus ring, fonts, colors, and spacing retained. Mobile inspection also shows the custom-session scope header consuming several rows; include that in the remaining responsive polish pass.
- Commit locally only. Goal remains active. Next concrete work: close the storage-unavailable form Back gap and verify actual offline/expired-session practice recovery, followed by induced page errors, creation/import interruption, responsive stress checks, and the complete production-build/database/browser baseline. Structured prompt and the numbered implementation scope remain intact.

### Storage fallback and offline save checkpoint

- Item 7: draft writes now fall back to browser-tab memory when sessionStorage is unavailable. The latest edit takes precedence over stale stored data; a failed removal leaves a tombstone rather than restoring an old draft. Account/scope keys, schema validation, baseline matching and the size bound remain enforced. No server-global draft storage is used.
- In-app navigation/Back can recover these drafts. Refresh or closing the tab still requires a beforeunload warning because tab memory cannot survive a document reload. Restored fallback drafts retain that warning. Copy now says to save before refreshing or closing; explicit Discard remains available.
- The new unit regression reproduced older stored content replacing a later failed write before the fix. Browser coverage verifies Back restoration, warning visibility, discard and a second navigation without resurrecting edits. Initial browser failure was a test issuing Back before the second Dashboard navigation completed; the corrected test waits for the destination URL and passes without increasing assertion timeouts.
- Actual browser-offline checks now pass for normal and custom practice: after Check, disconnect and Continue restores the checked question, database attempt count remains zero, and reconnect/retry advances with exactly one persisted attempt. These tests do not establish expired-session behavior.
- Final focused browser run: 7 passed including isolated Clerk setup/cleanup. Inspected the 390px reminder form screenshot: readable warning and controls, no overflow. TypeScript, targeted lint, whitespace checks and all 1,137 unit tests passed.
- Design critique/fixes: stopped stale draft restoration; avoided claiming tab memory survives reload; retained explicit discard; kept recovery copy near the form actions; preserved readable mobile layout and the established visual system.
- The preceding read-only audit yielded additional source-backed findings but did not implement them. This continuation returns to the original 20-item objective and release checks. Remaining work includes expired-session recovery, generation/import interruption and retries, induced route errors, real-device/zoom/content stress, and the full production-build/database/browser baseline. No push or PR; goal remains active.

### Ended-session practice recovery checkpoint

- The interruption check reproduced a real gap in normal and custom practice: after authentication was lost, a failed Continue restored the answer but offered only another save attempt. Added a bounded, non-cacheable sign-in status check after save errors. It exposes only a boolean; network/server failures do not imply an ended session.
- When authentication has ended, the restored question shows Sign in to continue, receives keyboard focus, disables the unavailable save action, and removes the obsolete generic retry message. The sign-in link loads a fresh document to reset stale authentication UI while preserving tab-local answer recovery. The primary recovery action stays on the right, including at 390px.
- Sign-in now accepts a validated relative destination for known product routes, retaining custom session query parameters. External, protocol-relative, malformed/non-string and unsupported destinations fall back to Dashboard. Existing WorkOS completion remains allowed. Successful reauthentication returns to the original question without test-driven navigation back to Practice.
- Browser fixtures revoke only isolated test-user sessions, clear cached cookies, submit the saved answer, and use the real email-code sign-in form. Both modes verify zero attempts before reauthentication, restored checked feedback afterward, and exactly one attempt after Continue. This induces authentication loss; it is not a wall-clock JWT-expiration or production Clerk deployment test.
- Test investigation: the original expectations reproduced missing sign-in recovery. Clerk's programmatic sign-in helper then raced its redirect and development-client handshake; the final tests use the actual form and wait for code preparation before entering the test code. Clearing only session cookies did not reliably expose an ended status in the development handshake; final fixtures clear cookies after server revocation. Production-mode authentication remains part of the full release baseline.
- Combined auth/offline run passed 6 tests including setup/cleanup. Final auth rerun passed 4 tests including setup/cleanup; inspected the 390px custom and 1280px normal recovery captures. The mobile notice is unobscured and the sign-in action is right-aligned. Full unit suite passed 1,159 tests; TypeScript, targeted lint and whitespace checks passed. Added tests cover redirect restrictions, authenticated/unauthenticated non-cacheable status responses, and offline/server-error distinction.
- Design critique/fixes: replaced an endless generic retry with a named recovery action; preserved the custom session destination; avoided exposing identity data in the status response; removed a stale toast obscuring the mobile notice; and kept the primary action on the right with keyboard focus. No brand changes.
- Previous continuation was progress (commit 6bf8ec3 plus offline proof). This continuation implements and verifies the next interruption requirement. Goal remains active; creation/import interruption, induced route errors, real-device/content/zoom checks and the complete release baseline remain. No push or PR.

### Creation interruption and material processing checkpoint

- Resumed the original goal after a read-only audit. The audit was progress through new source/rendered evidence, not implementation completion. The original 20 items and release checks remain required.
- Single-skill creation now warns before leaving through an app link or refreshing during the pending request. The processing screen explains that the page should remain open and that users should check Skills before resubmitting after leaving. A dropped text-creation action now returns to the editable saved text rather than throwing into the route error boundary. The recovery message acknowledges that the server may already have completed the draft; it does not automatically repeat the request.
- Material processing copy now explains that background work continues after leaving. Removed implementation terms such as retrieval chunks. The Materials library still excludes first imports without a ready revision; finding those imports from the library is a known remaining recovery gap, not solved by this copy change. Do not promise that the current library lists processing or failed imports.
- Browser evidence: the creation test reproduced the route-error failure before the catch was added. Final combined run passed four tests including isolated setup/cleanup, covering canceled navigation/refresh, dropped-request text recovery, refresh preservation, material Back/refresh, and a simulated persisted worker failure that appears through automatic polling. Final creation screenshot run passed three tests including setup/cleanup. Material test uses a 60-second overall budget for multiple document navigations and the polling transition; individual assertions remain bounded. Cleanup catches browser navigation failure so database cleanup still executes. The previous 30-second scenario timeout was not a worker failure.
- Database evidence: material-ingestion and material-drafting integration suites passed all 83 tests in 364 seconds, including partial failures, retry/idempotency, retained successful siblings, and bounded large website processing. They use isolated fixtures and fake external providers; they do not prove a real 100 MB upload or a full production worker deployment.
- Full unit suite passed 1,159 tests; TypeScript, targeted ESLint, and whitespace checks passed. Inspected 390px creation/material processing and 1280px recovered-form captures. Copy fits and original text remains editable. The recovered-form capture caught notification/step transitions, so it is not a settled-state animation audit.
- Design critique/fixes: replaced the blank error route with the user's existing input; avoided an automatic duplicate submission; added clear leave guidance; kept background-processing copy distinct from page-request creation; removed internal extraction jargon. Existing visual language preserved.
- Remaining: processing/failed import discoverability, actual refresh during completed server generation, induced route-error retry checks, real-device/long-content/zoom/screen-reader checks, and the complete production-build/database/browser baseline. The audit's other concrete workflow findings remain recorded in the conversation. Goal stays active. Commit locally only; no push or PR.

### Unfinished material discoverability checkpoint

- Previous goal turn was progress: commit 07fe45c fixed dropped creation requests and added interruption evidence. This continuation closes the missing-library destination discovered by that work.
- Materials now includes owned queued, processing, failed and ready revisions, excluding deleting materials and upload reservations that have not been submitted. The recovery view uses the latest visible revision; the existing reusable-material picker continues using only the ready active revision. A failed refresh therefore remains discoverable without removing the older ready revision from reuse. Skill links still count across revisions, preserving the existing contract.
- Library rows distinguish Waiting to process, Processing, Needs attention and Ready. Processing/failed rows explain the next action, expose View status/Review import, and update automatically through the existing status poller. The failed-row primary action is right-aligned; Delete is secondary. Suppressed meaningless missing-value placeholders and zero-skill metadata on unfinished imports. Dates say Updated until a material has actually been used.
- The processing detail now links explicitly back to Materials. This destination is proven available for submitted unfinished imports; unsubmitted upload reservations intentionally remain excluded because their file transfer may not have completed.
- Tests: new database regression failed against the prior ready-only library. The final regression verifies queued/processing/failed/ready visibility, exclusion of unsubmitted reservations, owner isolation, deleting-material exclusion, and preservation of an older ready revision while the recovery library shows its failed refresh. Full materials-foundation suite passed all 18 tests. Full unit suite passed 1,159 tests. TypeScript, targeted lint and whitespace checks passed.
- Browser scenario passed all three tests including isolated-account setup/cleanup. It leaves the import detail, finds the processing import through Materials, reopens and refreshes it, observes a simulated worker failure through library polling, opens Review import, and verifies Retry import and one persisted revision. This is navigation and status evidence; actual worker retry/idempotency remains covered separately by the preceding 83 integration tests, not by clicking Retry in this scenario.
- Visually inspected the library at 390px (failure) and 1280px (processing). Status, next action and content remain legible; actions wrap without clipping. Design critique/fixes: no missing processing item; no false Ready badge; no Delete-only action emphasis; no fake Used timestamp on untouched imports; no empty size or outline placeholders implying finished content. Existing palette, typography and layout retained.
- Goal remains active. Still required: remaining creation-completion recovery, induced route-error retry, real-device/content/zoom/screen-reader checks and full production-build/database/browser release verification. Other concrete findings from the read-only audit remain pending. No push or PR.

### Context-preserving links and skill loading checkpoint

- Previous goal turn was progress: commit 1394804 implemented and verified unfinished material discovery. This continuation addresses the audit's navigation-context and nested loading findings while preserving the full release objective.
- Full history on both skill-detail variants now links to History with that skill selected. Collections have a View skills link using the collection ID and All statuses, so paused/archived skills remain visible when browsing a collection's contents. Practice remains the rightmost primary action.
- Skill-detail loading now follows Schedule, Practice results, Recent reviews, Practice guidance, matching the populated page's ordering and vocabulary. It reserves the Rename/Move control row and uses compact guidance placeholders. Data-dependent empty sections may still differ in height; this is not a claim that every possible skill state has an exact skeleton match. The separate Add parent-boundary flash remains pending.
- The new loading-order regression failed before implementation. Full unit suite passed 1,160 tests afterward. TypeScript, targeted ESLint and whitespace checks passed. Extended authenticated detail scenario passed all three tests including setup/cleanup: it completes an owned fixture review, follows Full history to the scoped filter, opens the collection's skills and verifies the expected skill and All statuses. Existing empty/populated detail assertions also pass.
- Inspected Collections at desktop width and 390px. View skills is legible, actions wrap without horizontal clipping, and Practice stays last/full-width on mobile. Existing detail scenario captures 390/1280 empty-detail states. Browser resizing was reset afterward.
- Design critique/fixes: stopped losing skill context at History; supplied a direct collection-content destination; avoided hiding non-active skills in that destination; corrected stale loading labels/order; reserved the newly added organization controls. No brand changes.
- Goal stays active. Next required work includes induced route-error retry, remaining creation-completion recovery, real-device/long-content/zoom/screen-reader checks, and the full production-build/database/browser release matrix. Reminder save consistency, custom-session resume/preparation, Add loading and other audit findings remain pending. No push or PR.

### Route recovery and production verification checkpoint

- Previous goal turn was progress through a read-only audit: current source and rendered screens identified reminder save inconsistency, custom-session preparation/resume gaps, time formatting, and remaining secondary-screen polish. It did not implement those recommendations or replace the original 20-item objective. This continuation reread the original objective and resumed its verification work.
- Item 3: induced a failed Collections server action before backend delivery, then verified the actual route error boundary at 390px and 1280px. The error heading receives focus, navigation remains available, Try again restores unsaved collection text, and the dashboard escape works. The collection remains active. Shared-component unit cases cover active-navigation and escape destinations for all main route families. This is not a claim that every route was separately crashed during server rendering.
- Visual inspection found the shared error heading too small and its actions touching. Reused the existing error typography and added a full-width wrapping action row with a 10px gap and right-aligned primary action. Final desktop/mobile captures show the corrected layout; the browser test asserts the Retry button's right edge. Design critique/fixes: legible error hierarchy, separated buttons, primary action on the right, visible escape route, and retained draft/focus instead of a dead end. No brand changes.
- Development browser run passed four tests including account setup/cleanup. The old installed dependency tree passed a build, but inspection found Next 16.2.11 and Vitest 4.1.8 behind the committed lockfile. Ran npm ci without changing the manifest or lockfile. Under the locked Next 16.3.4 and Vitest 4.1.11 versions, all 1,166 unit tests, full lint, Prisma validation and production build pass. Earlier Prisma generation succeeded; reverted only its verified trailing-whitespace-only generated output. Strict deployment environment validation was not enabled by the local build.
- Production browser verification initially stopped in authentication setup because the localhost production process had no alpha allowlist. Provisioned two isolated managed test accounts and passed their exact emails only to the local server process, without changing app access policy or deployment configuration. A loopback-only hostname attempt caused internal proxy failures and was stopped; the standard localhost server responds successfully. The production flow/error run uses the existing manifest with --no-deps and a separate /tmp output directory; explicit account cleanup is required afterward.
- Final production run passed all 16 scenarios in 3.2 minutes: stale-buffer replacement, advance-before-acknowledgement, lost-save idempotency, normal/custom checked-answer restoration, queued navigation, deferred second-answer recovery, normal/custom Back during saving, the real 15-second confirmation timeout, and induced route errors at both widths. Inspected the production error screenshots. Anonymous production suite passed all 15 tests, including protected routes and auth-shell checks. This supersedes the earlier interrupted setup attempt; no app authentication bypass was added.
- Explicit managed-account/database cleanup completed after production verification. Stopped the temporary production server and restored npm run dev with the jobs watcher, using the locked dependencies. Verification logs: /tmp/ui-release-lock-unit.log, /tmp/ui-release-lock-lint.log, /tmp/ui-release-lock-prisma.log, /tmp/ui-release-lock-build.log, /tmp/ui-release-production-flow.log, /tmp/ui-release-production-anonymous.log. Production captures remain under /tmp/lr-production-e2e.
- Goal remains active. Full database and authenticated-browser release suites, remaining creation-completion recovery, real-device keyboard/long-content/text-zoom/screen-reader evidence and unresolved completion/preparation states remain required. This checkpoint proves the listed scenarios only, not the entire original objective. No push or PR; unrelated docs/product-discovery remains untouched.

### Custom-session preparation recovery checkpoint

- Previous goal turn was progress: commit 5f9b12c verified route recovery and 31 production browser scenarios on the locked dependencies. This continuation reread the full original goal and addressed the remaining preparation dead end in item 14.
- Found a server defect behind the missing recovery action: createCustomPracticeSession returned preparing for an empty plan, but the first present call marked that same session completed. Added a failing database regression before the fix. An initially empty session now stays active with zero completed exercises while readiness is checked. Later prepared inventory fills the same session; checks before readiness do not record answers or consume introductions. Existing tests still verify completion after exhausted/ineligible existing plans, scheduled eligibility and ownership boundaries.
- Preparing and daily-limit custom states now offer Check again using the existing authoritative presentation action and session ID. Failed requests keep the session and retry control. A newly ready question receives keyboard focus. Preparation is checked explicitly rather than by background presentation polling, which could introduce a skill without a visible user action.
- Waiting-state copy no longer claims work is necessarily running or exposes verified-compatible/scope jargon. It explains that the session is saved and points to Needs attention. Removed competing new-session/navigation buttons from this state, replaced duplicate status paragraphs with one updated message, corrected singular exercise copy, and fixed the inherited two-column action placement and low-prominence heading. Inspected final 390px and 1280px captures: two well-spaced actions, primary on the right, no overflow.
- Verification: full custom-session database suite passed all 16 tests. Initial full run had 15 passing plus one obsolete copy assertion; updated that assertion and reran the entire suite successfully. All 1,166 unit tests, TypeScript, targeted ESLint and whitespace checks passed. Combined completion/preparation browser run passed four tests including setup/cleanup. Final expanded preparation run passed three including setup/cleanup: persisted empty-session refresh, repeated readiness check, dropped request recovery, daily-limit enforcement then recovery, same session ID, focused first choice, and zero answer attempts. Browser fixtures simulate inventory becoming available; they do not prove live generation-worker timing.
- Design critique/fixes: false completion, four competing buttons, duplicated waiting prose, muted heading, and incorrect singular count were corrected without changing the design system. Goal remains active. Full release-wide database/authenticated-browser runs, remaining creation-completion interruption, real-device/long-content/zoom/screen-reader verification and other outstanding original requirements remain. No push or PR.

### Explicit reminder saving and full-suite checkpoint

- Previous goal turn was progress: commit 9671ed5 corrected empty custom sessions and verified preparation recovery. This continuation reread the original objective and started the complete database suite, then addressed a remaining item 7 draft-protection inconsistency.
- The reminder toggle previously submitted every current field immediately despite the explicit Save changes instruction. A new component test failed on that automatic action. Toggling now updates only the existing protected draft; Save changes submits it and Discard restores saved values. Removed toggle-only save/revert/notification branches, and clarified that Save applies all reminder preferences.
- All 1,167 unit tests passed. TypeScript, targeted lint and whitespace checks passed. Seven focused authenticated browser tests passed including setup/cleanup: practice/reminder draft restoration, failure recovery, unavailable-storage Back, timezone draft copying, pointer/keyboard toggle changes, no POST or database change before explicit save, refresh retention, discard, and one persisted save. The first new browser attempt targeted the visually hidden checkbox under its decorative switch; changed the test to click the visible label and exercise Space on the focusable input. No forced click or app hit-target change was used.
- Inspected the 390px reminder draft capture: toggle, restored-draft explanation, Discard and right-aligned Save remain usable. Design critique/fixes: surprise saves, coupled submission of unrelated edits, conflicting save instructions, toggle-specific success messages, and toggle rollback semantics were removed in favor of one existing save/discard model. No visual-system changes or extra controls.
- Full database suite is still running in exec session 5645, log /tmp/ui-release-full-db.log; its process was verified live. Full authenticated browser suite is running in exec session 22084, log /tmp/ui-release-full-auth.log, using one worker and isolated managed accounts. These are ongoing checks, not passes. Do not restart solely because a poll times out. Do not run unit tests (they alter the Clerk test manifest) or edit app code while the browser suite is active. Poll these exact handles on the next continuation and inspect failures before further edits. The dev server/jobs worker remain active on port 3000.
- Goal remains active with the complete original scope. Creation-completion interruption, real-device/long-content/text-zoom/screen-reader evidence and the remaining full release verification are not yet complete. No push or PR; preserve docs/product-discovery.

### Mobile verification scope clarification

- The user explicitly waived physical-device checks as a completion blocker: use the best available simulation and do not hold up work if real-device checks are unavailable. Paired devices were unavailable; no further connection request is needed. Long-content, enlarged-text and mobile-browser simulation remain useful release checks.
- Installed the WebKit build matching the repository's Playwright version (26.4, revision 2287). Initial cached WebKit builds belonged to other Playwright versions and could not launch through this project. No package or lockfile changes were made.
- Initial WebKit smoke checks passed on iPhone 13 (390×664) and iPad Pro 11 (834×1194) profiles: sign-in loads, the email field accepts text and retains focus, and the document has no horizontal overflow. Inspected the iPhone capture at /tmp/lr-iPhone-13-signin.png; the form and focus indicator fit. These are simulated browser checks, not physical software-keyboard or VoiceOver evidence. No sign-in was submitted in these smoke checks. Authenticated long-content and enlarged-text checks remain pending.
- The full database and authenticated suites are still running in sessions 5645 and 22084. Browser progress passed test 53 at this checkpoint. Confirmed failures to repair after the invocation: app-shell still expects the obsolete Organize practice heading at both widths; custom-preparation final SQL counts all account sessions, including a completed session from an earlier test, instead of scoping to this scenario. Its ready-state and focus assertions passed. The database suite reports one MCP custom-session wrapper mismatch; inspect the final failure detail before changing its expected mixed-review behavior. No app changes or unit runs were made during the active browser suite.
- Goal remains active. This continuation made verification progress and clarified the physical-device scope; it does not claim full release completion. Preserve ongoing suite handles and unrelated docs/product-discovery. No push or PR.

### Full-suite findings and mobile content checkpoint

- Previous turn made progress through WebKit setup and scope clarification. This turn reread the complete objective and polled the original running handles until terminal. Full database run finished with 522 passing tests and one obsolete mixedReview:false expectation across 41 files. Updated only the expected normalized scope to true, retaining the legacy false input to prove normalization. The complete agent-library-management file then passed all 12 tests (34.28 seconds). Logs: /tmp/ui-release-full-db.log and /tmp/ui-release-agent-library-recheck.log.
- Full authenticated run finished with 86 passing and 16 failing scenarios (18.7 minutes), including cleanup. Failures: two stale Collections headings; one preparation assertion counting prior account sessions; a flagged-question assertion treating structured paragraphs as one text node; two loading-geometry mismatches; two unscoped Archive selectors; two prompt-hierarchy scenarios (wrong custom scope and leaked daily limit); three report scenarios blocked by leaked daily limit; one select test starting from a preference saved by an earlier test; two UI-polish tests trying to access an archived skill in Active view. These results are not a clean browser baseline.
- Updated test expectations/selectors to target their actual fixture and current product semantics. Lifecycle fixtures now reset dailyNewSkillLimit and practicePreference before providing fresh skills, so completion/settings tests cannot gate subsequent tests. Custom prompt tests now preselect the actual skill. Archived-skill deletion scenarios explicitly open Archived. The dashboard skeleton also lacked the new Recent accuracy description; added its placeholder. Remaining geometry differences must still be inspected and fixed if the rerun reproduces them.
- Added mobile-content scenarios plus a focused authenticated-mobile-webkit project. Both Chromium and WebKit passed long skill titles, long choice text, inline and horizontally scrollable display math, 200% text-size stress, selection/checking, and typed Enter submission with a 390×430 viewport approximating space above a keyboard. Six tests passed including setup/cleanup in 33.6 seconds. This is simulated text enlargement and viewport reduction, not OS Dynamic Type, a real software keyboard, or VoiceOver evidence. Physical-device testing remains waived by the user.
- WebKit initially exposed a race in the shared test sign-in helper: Clerk redirected from / to Dashboard while the helper issued a second Dashboard navigation. The helper now waits for the existing redirect; both browsers passed afterward. Inspected Chromium enlarged-text and WebKit normal/short-viewport captures: choices and instructions wrap, the focused input is reachable, and Check remains available by scrolling. No design-language changes. Test artifacts will be overwritten by subsequent Playwright runs; result log is /tmp/ui-release-mobile-content-final.log.
- TypeScript and targeted lint passed before the final test-helper update; lint/diff checks cover the new test. No unit run occurred during authenticated testing. An affected-file authenticated rerun is now active in exec session 54969, log /tmp/ui-release-browser-recheck.log. Poll this exact handle; do not edit app code or run unit tests until it finishes. The former full-run handles 5645 and 22084 are terminal and closed.
- Goal remains active: finish browser failures and verification, latest-code lint/unit/build baseline, remaining import/creation interruption evidence and final requirement-by-requirement audit. This checkpoint does not treat narrower passes as full completion. Dev server/jobs still running. Commit locally only; preserve docs/product-discovery; no push or PR.

### Final requirement audit

The original numbered scope is implemented. The evidence below combines the full-suite run with the corrected affected-file reruns; it does not describe the earlier 86/102 browser run as a clean pass. Physical-device checks were explicitly replaced by best-effort simulation at the user's request.

| Requirement | Current implementation and verification |
| --- | --- |
| 1. Preserve checked reviews | Account-scoped normal/custom review recovery. Fluid-practice, offline and ended-session browser scenarios verify restoration and exactly one persisted review after retry. |
| 2. Slow-save navigation | Queued destination, bounded acknowledgement wait and recovery. Fluid-practice covers rapid advances, late/lost acknowledgement, normal/custom Back and timeout recovery; 16 production flow/error checks also passed earlier. |
| 3. Page errors | Shared branded boundaries retain navigation, focus the heading, offer Retry and an escape. Corrected page-error scenarios passed at 390/1280 with retained collection draft and no accidental archive. |
| 4. Practice this skill | Detail action preselects that skill in Custom session. Lifecycle and detail scenarios verify scoped navigation and practice. |
| 5. Report before answering | Normal, practice-only and scheduled-custom reporting expose the control before Check and preserve schedule/no attempts. All three report-before-answer scenarios passed in the corrected combined run. |
| 6. Accurate dashboard metric | Recent accuracy includes answer count and 14-day window. Dashboard-clarity scenario and current page source verify label/semantics. |
| 7. Preserve forms | Settings, reminder, single/batch creation, collections, skill editors, guidance and custom setup use protected drafts. Dedicated draft suites cover refresh, navigation, failure, discard and explicit save; storage-disabled and reminder-toggle cases also pass. |
| 8. Useful History | Question, learner answer, correct answer and explanation appear together; result badges do not truncate. Lifecycle review-detail/export scenario passed in both full and corrected runs. |
| 9. Skills search/filter | Combined title/collection/status filters and return context. Large-library browser scenario passed with populated fixtures and mobile/desktop checks. |
| 10. Rename/move | Explicit actions retain unsaved edits and update the library. Skill-organization scenario passed; no recreation required. |
| 11. Custom filter preview | Eligible-skill count, intersecting filters, preserved hidden selections and empty-match recovery. Custom-setup browser and domain tests passed. |
| 12. Stable mobile navigation | Fixed order plus Pages menu. All navigation/header/loading checks passed after unifying Pages at 44px and 15px on every route. |
| 13. History pagination/filter | Skill/collection/result filters and Load more. Older-mistake lifecycle scenario passed. |
| 14. Completion states | Next due time/custom action, preparation and daily-limit states remain distinct. Normal completion and same-session custom preparation/retry/focus tests passed, with database readiness/allowance checks. |
| 15. First import layout | Empty reuse panel no longer consumes the working area. Import-entry layout passed across tested widths. |
| 16. Vocabulary | Collections/materials/skills labels and relationship copy are consistent in the revised flows. Import vocabulary and corrected core-route tests passed. |
| 17. Dashboard prompt/actions | Shared PracticePrompt, formatted blanks, revised copy and Manage collections action. Dashboard-clarity and structured-prompt scenarios passed. |
| 18. Detail density | Schedule/results precede compact empty guidance. Empty/populated detail and context-preserving links verified in skill-detail-density. |
| 19. Defaults/timezones | Clear unlimited/default summaries and Use practice timezone. New-skill-limit, retention and settings draft/browser checks passed. |
| 20. Structured prompts | Validated instruction/content stored for new exercises and carried through quality/persistence/practice/recovery, with safe legacy fallback. Full domain suites and structured-prompt browser scenario passed. |

Release-check coverage: modal close accessible names, focus return, next-question live-region contracts, typed Enter/IME handling, offline/ended-auth/Back recovery, in-flight creation leave warning and retained inputs, unfinished custom-session refresh, background material leave/discovery, worker-failure visibility, idempotent retry and retained successful siblings. Integration tests cover long PDF rejection/Materials guidance, reusable storage limits, large website selections in bounded groups, partial generation failures and completion despite ambiguous event delivery. These use controlled providers/storage; no claim is made of a live 100 MB cloud upload or live-model reliability. Creation interruption is tested with a deliberately dropped request; a browser refresh after live-model completion was not separately exercised. The original interruption check is covered by refresh/navigation protection and retained work, not a claim that every possible server timing was reproduced.

Mobile evidence: Chromium and WebKit passed long titles/answers, inline/display math, doubled computed text and reduced viewport height for typed input. Inspected captures show wrapping and reachable controls; long display math scrolls within its own region. Simulation does not reproduce actual iOS keyboard behavior, VoiceOver speech or OS Dynamic Type. Those physical-device checks are deferred per the user's explicit instruction and do not block this release task.

### Final navigation correction and baseline

- Previous turn made concrete progress: database correction plus verified mobile tests and affected-file test fixes. This turn's combined browser rerun passed 33 of 34; only mobile loading geometry remained. Diagnostic measurements found the Pages control was 44px on Skills/Practice but 40px on Dashboard, changing the topbar by four pixels. Applied a shared topbar-specific 44px/15px rule. Removed temporary diagnostic logging. Final navigation/header/loading run passed all 13 tests, including all original geometry assertions and route-width checks at 390/720/1000/1119/1280. Inspected mobile loading/loaded captures. No tolerance was relaxed.
- The 16 failures from the original authenticated run are resolved across the 33-pass affected-file run and the final 13-pass navigation run. The database baseline is 522 passing plus the corrected entire 12-test agent-library file passing. Mobile Chromium/WebKit run passed six including setup/cleanup. Logs: /tmp/ui-release-browser-recheck.log, /tmp/ui-release-navigation-final.log, /tmp/ui-release-agent-library-recheck.log, /tmp/ui-release-mobile-content-final.log.
- Latest code passed all 1,167 unit tests, full lint, Prisma validation/generation and Next 16.3.4 production build. Generated Prisma differences were verified to be trailing whitespace only and restored; no schema/generated semantic changes. Logs: /tmp/ui-release-final-unit.log, /tmp/ui-release-final-lint.log, /tmp/ui-release-final-prisma.log, /tmp/ui-release-final-generate.log, /tmp/ui-release-final-build.log. Local build uses development service configuration and is not deployment approval or production-secret validation.
- Production anonymous-route smoke passed all 15 tests in 19.9 seconds against the latest build (/tmp/ui-release-final-anonymous.log). Stopped the temporary production server and restored npm run dev with the jobs watcher. Final verification is complete for the original scope with the simulation/provider limitations stated above. Commit locally only; no push or PR; unrelated docs/product-discovery stays untouched.

### Completion-card layout follow-up

- User screenshot exposed an unassigned next-review paragraph sizing the first column of the completion grid and pushing the main message/actions right. Replaced the two-column composition with one content column and moved the review time into the message group. Removed the decorative checkmark tile and its button-like shadow. Retained semantic heading/time, status announcements, recovery actions and sample-data controls.
- Saved confirmation is now plain green status text above the action row instead of a competing bordered badge. Actions span the card and remain right-aligned; the development footer uses the same full width. Next-review text is 16px and stays directly below the explanatory message.
- Visual critique/fixes: removed the false button affordance, eliminated the oversized empty first column, grouped the orphaned time with its message, reduced the competing saved badge, and aligned the actions/footer to the card edges. Existing fonts/colors/button system preserved.
- Verified completion/preparation/daily-limit states at 390, 820 and 1280px; inspected completion phone/tablet and daily-limit desktop captures. Added assertions for one grid column, message/time left alignment and actions below the copy. Three browser tests passed including setup/cleanup. All 1,167 unit tests, TypeScript, targeted lint and diff checks passed. No scheduling or persistence behavior changed; a new production build was not required for this bounded layout correction.

### One navigation mode per screen size

- Replaced the compact header's clipped horizontal link strip plus Pages button with a single Pages menu below 1120px. At 1120px and above, the existing full sidebar remains and Pages is hidden. The compact header now has one row: wordmark, menu and account. Both modes retain the same destination order and current-page indication.
- Removed hover/pressed translation from the navigation menu button. Dropdown items have 44px minimum touch targets. Explicit browser bounding-box assertions verify that hovering the button or an item does not move it; keyboard opening, Escape focus return, current-page state and all destination visibility also pass.
- Added a browser navigation helper that chooses the visible menu or sidebar, updating affected scenarios so they exercise actual user controls rather than hidden links. Normal-save navigation remains tested through the sidebar; custom-save navigation now uses the menu at 390px and waits for acknowledgement before opening the queued destination.
- Verified 320/390/719/720/820/1000/1119/1120/1280 responsive boundaries through the relevant scenarios. Inspected closed headers at 390/1000 and the final open menu at 390. Full navigation/header/loading/app-shell run passed 23 tests. Final larger-touch-target and pending-save menu run passed nine including setup/cleanup. Unit suite passed all 1,167 tests; full lint, TypeScript and whitespace checks passed. No backend or scheduling changes.
- Design critique/fixes: removed the duplicated navigation mode; removed partially hidden destinations; removed the menu's competition with horizontal links; removed hover translation; enlarged touch targets. Existing palette, fonts, icons and sidebar styling preserved. Local commit only; no push or PR. Dev server remains available.

### Reversible skeleton reveal experiment

- Rollback baseline: `7d5b439` on `a/ui-polish`. Keep this experiment in one local commit; reverting it restores the previous loading behavior without touching other UI polish. No push or PR.
- Adapted the skeleton/content crossfade from https://transitions.dev/detail.html?t=skeleton-loader-and-reveal: 240ms, opacity plus a 4px blur, no translation or scaling. Existing shimmer, fonts, colors and layout remain unchanged.
- A shared observer recognizes both existing skeleton classes across route fallbacks, optimistic navigation, nested skill/material loaders, custom setup, needs-attention and client practice loading. It retains only visible placeholder rectangles for the outgoing layer; it never copies learner content. Loaded DOM remains immediately interactive and is neither delayed nor remounted. Already-buffered questions and ordinary updates do not trigger a reveal.
- Reduced motion skips the effect. New loading, scrolling, resizing and unmounting cancel it and remove the inert, pointer-transparent overlay. No additional dependencies or backend changes.
- Design critique: avoided staggered waits, page movement, animated navigation chrome, duplicate accessible content and replaying the effect on every answer. The experiment is limited to a visible loading-to-content handoff.
- Verification: all 1,167 unit tests, full lint and TypeScript passed. Browser handoff checks passed for all seven primary destinations at 390/1280px, including reduced motion, overlay cleanup, pointer transparency and no replay on ordinary updates. Existing width checks passed at 390/720/1000/1119/1280px. Inspected paused mid-reveal phone/desktop captures and the live dashboard/history. Consolidated the overlay's reduced-motion rule with the existing rules after the CSS contract test flagged a second media block. No new production build or database checks for this client-only experiment; the dev server remains running.

### Skip reveals for fast loads

- Added a 200ms eligibility threshold measured from the first visible skeleton. Content ready sooner appears immediately; no minimum display time or artificial delay is introduced. Continuous loading preserves its start time, and completed loads or viewport/motion resets clear it so separate quick loads cannot accumulate.
- Browser regression coverage first reproduced unnecessary reveals on four consecutive short skeleton handoffs. After the fix, those handoffs skip animation at 390/1280px; deliberately slower loading still reveals across all seven primary destinations. Reduced motion and overlay cleanup continue to pass. All four browser tests (including fixture setup/cleanup), full lint and TypeScript passed.
- Full unit suite passed all 1,167 tests. This timing-only adjustment leaves the original reveal experiment independently revertible; no production build or database changes were needed.

### Remove the Add-page loading flicker

- The Add menu contains two static choices, but both its optimistic navigation state and route fallback were replacing them with skeletons. Extracted the existing choices into one shared component used by the page and both loading paths. Add now shows the real links immediately without a shimmer/reveal, even if routing takes longer than 200ms. Other data-backed pages retain the timed reveal.
- Browser geometry checks exposed a second cause: broad library heading selectors leaked into the pending Add content, then stopped applying at navigation completion. Scoped those selectors to direct header children. The shared markup preserves existing copy, typography, icons and destinations; no new visual treatment.
- Add explicitly skips inherited reveals when leaving another loading page. Regression checks confirm zero animation calls and identical choice bounds before/after navigation at 390/1280px, including a deliberately slow outgoing skeleton. Inspected Add captures at both widths. Nine combined navigation/reveal tests passed, then all four final reveal tests passed with the interrupted-loading case. Full 1,167 unit tests, lint, TypeScript and whitespace checks passed. Dev server remains running; local commit only, no PR.
